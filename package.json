{
  "name": "signid",
  "version": "1.0.0",
  "description": "Tiny library to generate signed identifiers",
  "main": "src/index.js",
  "scripts": {
    "test": "standard && mocha"
  },
  "engines": {
    "node": ">= 8.0"
  },
  "keywords": [],
  "author": "pjhl <pjhl.coder@gmail.com>",
  "repository": "pjhl/signid",
  "license": "ISC",
  "devDependencies": {
    "assert": "^1.4.1",
    "husky": "^1.0.0-rc.6",
    "mocha": "^5.1.1",
    "standard": "^11.0.1"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm test",
      "pre-push": "npm test"
    }
  }
}
